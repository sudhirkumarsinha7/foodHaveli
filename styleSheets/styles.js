import { StyleSheet, Platform } from 'react-native';
export default StyleSheet.create({
      iconText: {
        fontFamily: 'Cochin',
        fontSize: 14,
        textAlign: 'center',
        color: '#FFFFFF',    
      },
      titleText: {
        fontSize: 20,
        fontWeight: 'bold',
      },
     
});
